import { GetChordMapPipe } from './get-chord-map.pipe';

describe('GetChordMapPipe', () => {
  it('create an instance', () => {
    const pipe = new GetChordMapPipe();
    expect(pipe).toBeTruthy();
  });
});
