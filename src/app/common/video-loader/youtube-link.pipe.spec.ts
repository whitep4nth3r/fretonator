import { YouTubeLinkPipe } from './youtube-link.pipe';

describe('YoutubeLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new YouTubeLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
