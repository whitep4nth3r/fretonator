import { YoutubeLinkHtmlPipe } from './youtube-link-html.pipe';

describe('YouTubeLinkHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeLinkHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
