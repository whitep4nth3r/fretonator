import { GetNoteFromMapPipe } from './get-note-from-map.pipe';

describe('GetNoteFromMapPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNoteFromMapPipe();
    expect(pipe).toBeTruthy();
  });
});
