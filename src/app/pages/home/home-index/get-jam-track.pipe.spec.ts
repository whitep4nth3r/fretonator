import { GetJamTrackPipe } from './get-jam-track.pipe';

describe('GetJamTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new GetJamTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
