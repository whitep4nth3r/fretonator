import { GetScaleFromModeMapPipe } from './get-scale-from-mode-map.pipe';

describe('GetScaleFromModeMapPipe', () => {
  it('create an instance', () => {
    const pipe = new GetScaleFromModeMapPipe();
    expect(pipe).toBeTruthy();
  });
});
