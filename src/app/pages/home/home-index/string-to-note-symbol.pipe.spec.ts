import { StringToNoteSymbolPipe } from './string-to-note-symbol.pipe';

describe('StringToNoteSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToNoteSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
